#include <iostream>

using namespace std;

int main() {
    int x, y, z;
    
    cin >> x >> y >> z;

    if (z != (x + y)) {
        cout << "Error. The sum of "s << x << " and "s << y << " is "s << x+y << endl; 
    }
}